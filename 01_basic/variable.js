console.log("Welcome to the Javascript Anurag"); //to print the value

// Variables
const id='anurag'; //we can not re-declare and re-assign the value to the const variables
let rollno=10; //we can re-assign the value but no re-declare the let variables
var state='delhi'//try to do not use var keyword to eclare the variables because of block scope and functional scope
pincode=110085; //we can declare the variable without using keyword but never used in pruduction level code

// id=10; we can not reassign an value to const variables;
rollno='ashutosh'
state=20
pincode="mumbai"
console.table([id,rollno,state,pincode]);